import Navigation from './Navigation';

function Header({handleSubmit}){

    return(
        <header>
            <h1>Photos</h1>
            <Navigation/>
        </header>
    );
}

export default Header;